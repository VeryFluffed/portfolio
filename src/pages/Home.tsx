import { AboutMe } from "@/components/About";
import { Hero, HomeProjects } from "@/components/Home";

const Home = () => {
  return (
    <main>
      <Hero />
      <AboutMe />
      <HomeProjects />
    </main>
  );
};

export default Home;
