import { AboutMe } from "@/components/About";
import { Hero, Projects } from "@/components/Home";

const Home = () => {
  return (
    <main>
      <Hero />
      <AboutMe />
      <Projects />
    </main>
  );
};

export default Home;
